version: "3.8"
services:
  app:
    build: .
    container_name: groq_deepgram_agent
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
      

  
